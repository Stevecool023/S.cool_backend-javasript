/*
 * The isFinite() Method
 * This glogal method returns false if the argument is Infinity or NaN
 * Otherwise it returns true
 */

// Example
console.log(isFinite(10/0)); // returns false
console.log(isFinite(10/1)); /* returns true */
